const add = (firstNumber, secondNumber) => {
    const sum = firstNumber + secondNumber;
    return sum
}

let sum = add(4, 14)


const multiply = (sum) => {
    const sum2 = sum * 2
    return sum2
}

let sum2 = multiply(sum, 2)

// result = multiply(sum1)
console.log(sum2)